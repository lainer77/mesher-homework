import { use<FTName | capitalize>init } from '_hooks'

import { <FTName | capitalize>Styled } from './<FTName | capitalize>.styled';

export default function <FTName | capitalize>() {
    use<FTName | capitalize>init();
    return (
        <<FTName | capitalize>Styled>
        </<FTName | capitalize>Styled>
    );
}
